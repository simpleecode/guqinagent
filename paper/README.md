# Paper draft

Compile with:

```bash
cd /Volumes/F/work/Gunqin-agent/paper
latexmk -pdf main.tex
```

The draft separates implemented claims from experimental claims. Fill red `TODO` cells only after final benchmark runs, and replace `references.bib` with verified sources before submission.

